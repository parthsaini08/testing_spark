name: Spark testing main

on:
  push:
    branches:
      - 'main'
    paths:
      - 'pipeline/Spark/src/use_cases/spark_price_at_an_instant.py'
  pull_request:
    types:
      - opened
      - reopened
    branches:
      - 'main'
      - 'dev'
    paths:
      - 'pipeline/Spark/src/use_cases/spark_price_at_an_instant.py' 

env:
      reponame: "$GITHUB_REPOSITORY"
jobs:
  testing:
    
    uses: $reponame/.github/workflows/testing.yml@main
    with:
      usecase_name: price_at_an_instant
        

      
      
